; ModuleID = 'grace program'
source_filename = "grace program"

declare void @writeInteger(i64)

declare void @writeString(ptr)

declare void @writeChar(i8)

declare i32 @readInteger()

declare i8 @readChar()

declare void @readString(i64, ptr)

declare i32 @ascii(i8)

declare i8 @chr(i64)

declare i32 @strlen(ptr)

declare i32 @strcmp(ptr, ptr)

declare void @strcpy(ptr, ptr)

declare void @strcat(ptr, ptr)

define void @main() {
entry:
  %d = alloca i64, align 8
  %a = alloca [5 x i64], align 8
  %adam = alloca i8, align 1
  %changed = alloca i64, align 8
  store i64 5, ptr %d, align 4
  %0 = load i64, ptr %d, align 4
  %addtmp = add i64 %0, 1
  store i64 %addtmp, ptr %changed, align 4
  store i8 97, ptr %adam, align 1
  %1 = load i64, ptr %d, align 4
  %2 = call i64 @hello(i64 %1, i64 2, i8 97)
  ret void
}

define i64 @hello(i64 %0, i64 %1, i8 %2) {
entry:
  %r = alloca i8, align 1
  %g = alloca i64, align 8
  %a = alloca i64, align 8
  store i64 %0, ptr %a, align 4
  store i64 %1, ptr %g, align 4
  store i8 %2, ptr %r, align 1
  store i64 2, ptr %g, align 4
  ret i64 5
}

define i64 @main.1() {
entry:
  call void @main()
  ret i64 0
}
