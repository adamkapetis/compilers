fun hello () : nothing 
var q : int;
fun add (i, j : int) : int {
      writeInteger(i);
      j <- i + 1;
      writeInteger(j);
      return j;
   }
{
   add(1, 2);
   $q <-1;
   $writeString("Hello world! hey\\n ");
   writeChar('a');
   writeInteger(q);

   $i <- strlen("Hello world");
   $writeInteger(i);
}